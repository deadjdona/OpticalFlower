# Betafly Optical Position Stabilization
# Requirements for Raspberry Pi Zero

# Core dependencies
spidev>=3.5  # For PMW3901 + // sudo apt install python3-spidev
RPi.GPIO>=0.7.1 # + // sudo apt install python3-rpi.gpio
smbus2>=0.4.0  # For Caddx Infra 256 (I2C) + // sudo apt install python3-smbus2

# Camera and computer vision
opencv-python>=4.5.0  # For camera-based optical flow // 600 Mb // sudo apt install python3-opencv
opencv-contrib-python>=4.5.0  # Additional CV algorithms ----------

# Web interface
Flask>=2.0.0       # Web server framework +++ // sudo apt install python3-flask
Flask-CORS>=3.0.0  # Cross-origin resource sharing ++++ // sudo apt install python3-flask-cors

# Optional dependencies for flight controller communication
pymavlink>=2.4.37  # For MAVLink protocol -------- // sudo apt install python3-pymavlink
pyserial>=3.5      # For serial communication (SBUS/MSP) // sudo apt install python3-serial

# Data processing and utilities
# numpy>=1.21.0      # For advanced math operations // sudo apt install python3-numpy

# Development and testing
pytest>=7.0.0      # For unit tests // sudo apt install python3-pytest
pytest-cov>=3.0.0  # For code coverage // sudo apt install python3-pytest-cov
